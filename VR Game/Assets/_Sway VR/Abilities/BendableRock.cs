using UnityEngine;

public class BendableRock : Health
{
    public enum E_BendState
    {
        idle,
        launched,
        spawning,
        colliding,
        destroyed,
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
